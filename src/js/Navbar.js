// Navbar.js — simplificado. La secuencia de entrada
// ahora la maneja CSS animation directamente en el componente.
// Este archivo se mantiene por si se necesita lógica futura.

export function setupNavbarSequence() {
  // No-op: animación delegada a CSS (navbarEnter keyframe).
  return () => {};
}
